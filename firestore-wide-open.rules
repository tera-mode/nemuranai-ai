rules_version = '2';

// 完全オープンルール（テスト専用）
// ⚠️ セキュリティリスクが高いので、テスト完了後は必ず適切なルールに戻してください

service cloud.firestore {
  match /databases/{database}/documents {
    // 全てのドキュメントに対して読み書きを許可
    match /{document=**} {
      allow read, write: if true;
    }
  }
}