rules_version = '2';

// 簡易セキュアルール - NextAuth + Firebase統合用
// 認証状態のみチェック、詳細なユーザーID照合は行わない

service cloud.firestore {
  match /databases/{database}/documents {
    
    // 認証済みユーザーのみアクセス可能
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}